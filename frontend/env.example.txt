# Environment variables for frontend
# Copy this to .env.local and fill in values

# Backend API base URL used by Next.js proxy route (server-side)
BACKEND_URL=http://localhost:8000/api

# Secret API key injected into requests to backend via proxy (server-side only)
# IMPORTANT: Must match backend app.config.Settings.api_secret_key value
API_SECRET_KEY=change-me-in-production

# Public API base used by browser code (points to Next.js proxy)
NEXT_PUBLIC_API_URL=/api

