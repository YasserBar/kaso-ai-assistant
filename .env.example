# Root .env example for Kaso AI Assistant
# Copy this file to `.env` at the repository root and fill values

# General
NODE_ENV=development

# Frontend public envs (exposed to browser)
NEXT_PUBLIC_API_URL=/api
# Use Next.js server '/api' proxy; it forwards to http://backend:8000 internally
NEXT_PUBLIC_API_KEY=

# Backend security and integrations
API_SECRET_KEY=change-me-in-production
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
GROQ_API_KEY=

# Backend model configuration (optional)
LLM_MODEL=llama-3.1-8b-instant
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
RERANKER_MODEL=cross-encoder/mmarco-mMiniLMv2-L12-H384-v1

# Persistence and database (optional)
DATABASE_URL=sqlite+aiosqlite:///./data/chat_history.db
CHROMA_PERSIST_DIR=./data/chroma_db
CHROMA_COLLECTION_NAME=kaso_knowledge

# RAG tunables (optional)
RAG_TOP_K=10
RAG_RERANK_TOP_K=5
CHUNK_SIZE=500
CHUNK_OVERLAP=50